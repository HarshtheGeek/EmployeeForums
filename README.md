# employee_forums_harsh

A new Flutter project.
```The API could not be fetched, unfortunately. I rechecked the code but still couldn't identify the error.```
``` Sign up using the given API is working perfectly```
